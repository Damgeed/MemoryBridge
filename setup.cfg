[metadata]
name = memory-bridge
version = 0.1.0
description = Cross-session memory persistence for multi-agent teams

[options]
packages = memory_bridge
package_dir =
    = src
include_package_data = True

[options.package_data]
memory_bridge =
    static/*.html
    static/*.svg
