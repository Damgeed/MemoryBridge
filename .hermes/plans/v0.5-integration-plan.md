# Memory Bridge v0.5 — Integration & Developer Experience

**Goal:** Make Memory Bridge easy for agents to integrate with. Full-text search + Python SDK + Prometheus metrics.

## Items

### Phase 1
**Henry 🧠** — FTS5 full-text search over memory content
- Add `memory_fts` virtual table in storage.py
- Sync on store/delete
- `search_memories(query, limit, offset)` method with relevance ranking
- `/memories/search?q=...` endpoint in main.py

### Phase 2 (Parallel)
**Fred 🚀** — Python client SDK
- `memory_bridge_client/client.py` — async Client class
- Wraps all 10+ endpoints
- Auto auth via api_key
- Type hints, docstrings
- Tests using the real server

**Fred 🚀** — Prometheus /metrics endpoint
- Add prometheus_client dependency
- Export: request_count, memory_count, session_count, avg latency
- `/metrics` endpoint alongside `/health`

### Phase 3
- Tag v0.5.0
