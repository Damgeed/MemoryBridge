# Memory Bridge v0.4 — Production Ready

**Goal:** Make Memory Bridge deployable in multi-worker production environments.

## Items

### Phase 1 (Parallel)
**Henry 🧠** — Shared metrics (eliminate module-level globals)
- Create `metrics` table in storage: (key TEXT PRIMARY KEY, value TEXT)
- Replace `START_TIME`, `request_count`, `total_latency`, `last_cleanup_at` globals
- Add `record_metric()`, `get_metric()`, `get_all_metrics()` to storage
- Update health endpoint to read from metrics table
- Tests verify metrics persist across reads

**Fred 🚀** — CORS + Request ID + Rate limiting
- Add `CORSMiddleware` (allow all origins for now, configurable)
- Add request ID middleware (uuid4 per request, inject into request.state)
- Add simple in-memory rate limiter (token bucket, configurable via env vars)

### Phase 2 (Parallel)
**Henry 🧠** — Production Dockerfile + docker-compose
- Non-root user
- HEALTHCHECK instruction
- Smaller base image (slim-buster or alpine)
- docker-compose.yml with healthcheck
- Graceful shutdown handling

**Fred 🚀** — Pre-commit CI + Documentation
- Add ruff to CI (lint check)
- Add pyproject.toml config for ruff
- Write comprehensive API reference in README
- Document all env vars in README

### Phase 3
- Full test suite
- Tag v0.4.0
