# Memory Bridge v0.3 — Harden & Ship

**Mandate:** No new features. Every line of code answers: *does this make v0.2.0 production-safe?*

## Tasks

### Phase 1 (Parallel)
**Henry 🧠** — Schema migration system + eliminate legacy dual-write
- Add `schema_version` tracking table
- Replace try/except migration blocks with version checks
- Drop legacy `tags` JSON column from memories table
- Update all storage methods to use junction table only
- Update `_row_to_entry` to fetch tags from junction table only

**Fred 🚀** — Bounded lock dict + Lineage depth warnings
- Limit `HandoffProtocol._session_locks` (LRU eviction or weak refs)
- Explicit warning when lineage reaches depth limit

### Phase 2 (After Henry)
**Fred 🚀** — Pagination + Cleanup monitoring
- Cursor/offset pagination on query endpoints
- `last_cleanup_at` timestamp, log warning if cleanup overdue
- Configurable via `MEMORY_BRIDGE_MAX_CLEANUP_INTERVAL`

### Phase 3
- Full test suite green
- Tag v0.3.0
- Team retrospective
